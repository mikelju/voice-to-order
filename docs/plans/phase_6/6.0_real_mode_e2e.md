# Phase 6: Real mode (live Whisper + Gemini) + E2E

> Functional spec: `docs/plans/phase_6/6.spec.md`

## Implementation steps

- [x] Step 1: synthetic test audio via Windows TTS (`.tmp/`, not committed) dictating a
      fictional order in Spanish — `tools/make_tts_audio.py`
- [x] Step 2: live E2E in `APP_MODE=real`: process-audio → search-articles → finalize →
      send; evidence in `6.1_real_run_evidence.md`. Deviation: in-process ASGI instead of a
      port-8020 server (equivalent; documented in 6.1). Bug found+fixed in the throwaway
      driver only (selector loop before `asyncio.run`), not in product code
- [x] Step 3: `tests/realmode/test_real_e2e.py` (opt-in via `RUN_REAL_MODE_TESTS=1`):
      automates the text-only stages (extraction, live-embedding search, re-rank); demo
      suite stays green without the flag
- [x] Step 4: cost estimate per order documented (6.1); master plan updated; commit

## Notes / Technical decisions

- The E2E audio is synthetic TTS (no biometrics) and lives in `.tmp/` (gitignored).
- Real mode reuses the same simulated delivery channels by design.
