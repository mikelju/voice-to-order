# Phase 7: Security audit + public README + release gate

> Functional spec: `docs/plans/phase_7/7.spec.md`

## Implementation steps

- [x] Step 1: full `/8-audit` over `src/backend/`, `tools/`, `src/frontend/`. Report ->
      `docs/security/audit-2026-06-17-full.md`, severity-ranked (0 Critical, 0 High,
      1 Medium, 5 Low, 3 Info + OBS-001 release-gate item).
- [x] Step 2: applied SEC-001 (audio upload cap + type allowlist), SEC-002 (no filename
      echo), SEC-003 (`allow_credentials=False`), SEC-004 (`python-multipart>=0.0.18`);
      added a 415 test. Suite green (117 passed, 1 skipped). Remaining Low/Info tracked in
      the report (non-blocking).
- [x] Step 3: public `README.md` written (what+why, demo vs real table, 2-command
      quickstart, real-vs-synthetic section, architecture, case-study link, local-only
      warning).
- [x] Step 4: CI — `.github/workflows/ci.yml`: backend tests on a pgvector service,
      structural anonymization gate, `pip-audit`, and a frontend build. Real-mode tests
      never run in CI.
- [ ] Step 5: pre-push gate (author-run, external secrets): `verify_anonymization.py
      --repo . --terms <external list>` + git-history sweep across all commits (OBS-001).
      Push to the public remote ONLY after this passes — and only on explicit author
      go-ahead (ASK FIRST: publishing). **Still pending.**

## Notes / Technical decisions

- The real-terms list and the HMAC salt live OUTSIDE the repo
  (`C:\Python Projects\portfolio-private\`); the `--terms` sweep and any id de-anonymization
  are author-machine only. CI runs the structural gate, not `--terms`.
- The audit is the release-gate audit deferred from Phases 3-5 (single pass over the whole
  surface, per the decision recorded in the master plan).
- Order: audit first (findings may change code/docs), then README/CI, then the sweep+push
  last. README and CI are independent and can proceed in parallel with fix work.
- Push is the only step that is both ASK FIRST and irreversible (public git history) —
  it is explicitly gated on the Step-5 sweep and author approval.
