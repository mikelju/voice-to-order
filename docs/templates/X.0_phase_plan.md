# Phase X: [Phase Name]

> Functional spec: `docs/plans/phase_X/X.spec.md`
> This plan implements the requirements defined in the spec above.

## Objective

[Copied or summarized from the spec — keep alignment]

## Success criteria

<!-- Mapped 1:1 from the spec's acceptance criteria -->

- [ ] [spec criterion US-1]
- [ ] [spec criterion US-1]
- [ ] [spec criterion US-2]

## Implementation steps

<!--
Each step = what to do + affected files + associated test.
/6-implement executes each step with the test-first cycle (test → red → code → green → [x]).
If there are >10 steps, create a separate X.tasks.md.
-->

- [ ] Step 1: [concrete description] → `path/file` — test: `tests/path/test_file`
- [ ] Step 2: [concrete description] → `path/file` — test: `tests/path/test_file`
- [ ] Step 3: [concrete description] → `path/file`, `path/other` — test: `tests/path/test_file`

## Affected files

| File | Change |
|---------|--------|
| `path/file.ts` | [what changes and why] |
| `path/other.ts` | [what changes and why] |

## Notes / Technical decisions

[Relevant design decisions: why this solution was chosen and not another.
Gotchas discovered during implementation. Accepted technical debt.]

## Fixes

<!-- References to fix-N applied to this phase's functionality -->

<!--
When all steps are completed, run /7-verify to check alignment with the spec.
If deviations appear during /6-implement, they are documented in X.Y_name.md
and this plan is updated to reflect the adopted solution.
-->
